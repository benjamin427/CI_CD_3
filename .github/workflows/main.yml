name: Cypress Tests
on: [push]
jobs: 
  cypress-run:
    runs-on: windows-latest
    steps:
       - name: Checkout
         uses: actions/checkout@4
       - name: Cypress.io
         uses: cypress-io/github-action@v6
         with:
          start: cypress run
